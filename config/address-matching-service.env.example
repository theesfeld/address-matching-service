# Uncomment and adjust values as needed. Lines beginning with `#` are ignored.

# Database connection string (libpq format).
# AMS_DB_CONNECTION=postgresql://citywide:excelsior!@citywideportal.io:5433/citywide

# Bind address / port
# AMS_BIND_ADDRESS=192.168.1.10
# AMS_BIND_PORT=8080

# Scoring thresholds
# AMS_STRUCTURED_THRESHOLD=0.65
# AMS_FUZZY_THRESHOLD=0.55
# AMS_LLM_THRESHOLD=0.70
# AMS_MAX_CANDIDATES=5

# Optional LLM command
# AMS_LLM_COMMAND=/usr/local/bin/address-matcher-llm-helper
